package proxy;

import com.gavin.model.Page;
import com.gavin.model.Response;
import com.sitexa.facade.interfaces.CommentService;
import com.sitexa.facade.vo.Comment;
import com.sitexa.facade.vo.Video;
import com.sitexa.service.impl.CommentServiceImpl;
import conf.BaseTest;
import org.junit.Test;

public class CommentServiceTest extends BaseTest {
/*

    CommentService commentService = new CommentServiceImpl();

    @Test
    public void saveComment() {

        Comment comment = new Comment();
        comment.setVideoId(1L);
        comment.setUserId(1L);
        comment.setUserName("xxnn");
        comment.setContent("asdfasldk;lsdka;lskdffalskdfaskdf;alskdf;alskdf;alskdff");
        comment.setIscomment(1);

        Response response = commentService.saveComment(comment);
        result(response);
    }

    @Test
    public void queryByVideoId() {
        int l = commentService.queryCommentByVideoId(1L);
        System.out.println("l = " + l);

        Video video = new Video();
        video.setId(1L);

        Page<Comment> pages = commentService.queryPageCommentByVideoId(0, 10, video);
        result(pages);
    }

    @Test
    public void queryByUser() {
        Response response = commentService.queryByUserComment(1L, 1L);
        result(response);
    }
*/
}
